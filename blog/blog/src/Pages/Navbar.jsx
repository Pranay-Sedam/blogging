import React from 'react';

import './Navbar.css';

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1 className="navbar-heading">Blogs</h1>
      {/* Add more links if needed */}
    </nav>
  );
};

export default Navbar;
